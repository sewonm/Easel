# Server port - The port on which the application will run (default: 3000)
PORT=3000

# App package name - Your application's unique identifier.  Make sure it exactly matches the package name in the MentraOS Developer Console
PACKAGE_NAME=com.yourname.artmentor

# MentraOS API key - Your MentraOS developer API key from the MentraOS Developer Console (never commit the key to source control)
MENTRAOS_API_KEY=your_mentraos_api_key_here

# HuggingFace API key - Your HuggingFace API key for computer vision models (get from https://huggingface.co/settings/tokens)
HF_API_KEY=your_huggingface_api_key_here